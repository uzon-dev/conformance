metric is {
    name is "heap_size"
    host is "server-09"
    timestamp is 1712808940 as u64
    value is 53.3
    unit is "bytes"
    tags is {
        environment is "development"
        region is "us-west"
        service is "svc-09"
    }
}
