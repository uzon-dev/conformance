// Schema definition #60
@type is "schema"
version is 5 as u8
name is "main_schema_60"
fields are
        { name is "field_0", @type is "i64", required is false },
        { name is "field_1", @type is "u16", required is true },
        { name is "field_2", @type is "f64", required is false },
        { name is "field_3", @type is "i64", required is true }
