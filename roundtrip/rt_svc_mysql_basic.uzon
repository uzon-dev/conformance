service is {
    name is "mysql"
    version is "3.3.0"
    port is 8003 as u16
    health is "/health"
    replicas is 4 as u8
    resources is { cpu_m is 400 as u16, memory_mb is 512 as u16 }
    labels is { app is "mysql", tier is "frontend" }
}
