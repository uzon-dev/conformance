metric is {
    name is "request_count"
    host is "server-10"
    timestamp is 1712803000 as u64
    value is 85.0
    unit is "count"
    tags is {
        environment is "development"
        region is "eu-west"
        service is "svc-00"
    }
}
