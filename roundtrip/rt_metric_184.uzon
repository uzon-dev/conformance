metric is {
    name is "network_out"
    host is "server-04"
    timestamp is 1712811040 as u64
    value is 12.8
    unit is "bytes"
    tags is {
        environment is "staging"
        region is "us-east"
        service is "svc-04"
    }
}
