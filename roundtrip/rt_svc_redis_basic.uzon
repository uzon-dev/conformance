service is {
    name is "redis"
    version is "3.1.1"
    port is 8001 as u16
    health is "/health"
    replicas is 2 as u8
    resources is { cpu_m is 200 as u16, memory_mb is 256 as u16 }
    labels is { app is "redis", tier is "backend" }
}
