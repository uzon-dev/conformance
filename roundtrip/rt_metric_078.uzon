metric is {
    name is "network_in"
    host is "server-18"
    timestamp is 1712804680 as u64
    value is 32.6
    unit is "bytes"
    tags is {
        environment is "production"
        region is "eu-west"
        service is "svc-08"
    }
}
