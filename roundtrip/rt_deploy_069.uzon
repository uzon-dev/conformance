deployment is {
    id is "deploy-069"
    environment is "staging"
    region is "ap-southeast-1"
    version is "4.9.4"
    replicas is 6 as u8
    image is "registry.example.com/app:4.9.4"
    resources is { cpu_m is 890 as u16, memory_mb is 1360 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-14T21:00:00Z"
    healthy is true
}
