metric is {
    name is "queue_depth"
    host is "server-14"
    timestamp is 1712810440 as u64
    value is 95.8
    unit is "count"
    tags is {
        environment is "production"
        region is "eu-west"
        service is "svc-04"
    }
}
