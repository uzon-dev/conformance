root is { level1 is { level2 is { level3 is { level4 is { level5 is { level6 is { level7 is { level8 is { level9 is { level10 is { level11 is { level12 is { level13 is { level14 is "leaf" } } } } } } } } } } } } } }
