metric is {
    name is "network_out"
    host is "server-09"
    timestamp is 1712810140 as u64
    value is 87.3
    unit is "bytes"
    tags is {
        environment is "staging"
        region is "us-west"
        service is "svc-09"
    }
}
