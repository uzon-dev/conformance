deployment is {
    id is "deploy-067"
    environment is "production"
    region is "eu-west-1"
    version is "4.7.2"
    replicas is 4 as u8
    image is "registry.example.com/app:4.7.2"
    resources is { cpu_m is 870 as u16, memory_mb is 1328 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-12T19:00:00Z"
    healthy is true
}
