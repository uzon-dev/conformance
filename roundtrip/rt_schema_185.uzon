// Schema definition #185
@type is "schema"
version is 4 as u8
name is "config_schema_185"
fields are
        { name is "field_0", @type is "bool", required is false },
        { name is "field_1", @type is "i64", required is true }
