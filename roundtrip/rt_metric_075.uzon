metric is {
    name is "cpu_usage"
    host is "server-15"
    timestamp is 1712804500 as u64
    value is 27.5
    unit is "percent"
    tags is {
        environment is "production"
        region is "ap-east"
        service is "svc-05"
    }
}
