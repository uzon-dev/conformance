metric is {
    name is "thread_count"
    host is "server-12"
    timestamp is 1712807920 as u64
    value is 24.4
    unit is "count"
    tags is {
        environment is "production"
        region is "us-east"
        service is "svc-02"
    }
}
