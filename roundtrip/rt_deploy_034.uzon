deployment is {
    id is "deploy-034"
    environment is "canary"
    region is "ap-southeast-1"
    version is "2.14.4"
    replicas is 3 as u8
    image is "registry.example.com/app:2.14.4"
    resources is { cpu_m is 540 as u16, memory_mb is 800 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-07T10:00:00Z"
    healthy is true
}
