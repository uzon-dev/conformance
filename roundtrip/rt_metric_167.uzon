metric is {
    name is "disk_io"
    host is "server-07"
    timestamp is 1712810020 as u64
    value is 83.9
    unit is "bytes"
    tags is {
        environment is "development"
        region is "ap-east"
        service is "svc-07"
    }
}
