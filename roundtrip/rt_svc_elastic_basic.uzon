service is {
    name is "elastic"
    version is "4.0.2"
    port is 8005 as u16
    health is "/health"
    replicas is 2 as u8
    resources is { cpu_m is 100 as u16, memory_mb is 256 as u16 }
    labels is { app is "elastic", tier is "infrastructure" }
}
