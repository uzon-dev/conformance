endpoints is [
    { path is "/envoy/api/v1/status", method is "GET", timeout_ms is 2200 as u32, auth is true },
    { path is "/envoy/api/v1/config", method is "GET", timeout_ms is 3200 as u32, auth is true },
    { path is "/envoy/api/v1/metrics", method is "GET", timeout_ms is 1100 as u32, auth is false }
]
