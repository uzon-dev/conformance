metric is {
    name is "latency_p50"
    host is "server-02"
    timestamp is 1712804920 as u64
    value is 39.4
    unit is "milliseconds"
    tags is {
        environment is "staging"
        region is "eu-west"
        service is "svc-02"
    }
}
