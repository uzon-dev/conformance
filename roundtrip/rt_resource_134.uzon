// Resource #134
@type is "cache"
name is "cache_134"
region is "us-east-1"
config is {
    @type is "cache_config"
    backend is "memcached"
    ttl_seconds is 84130 as i32
    max_memory_mb is 2803 as u16
}
tags are "beta", "main"
active is true
