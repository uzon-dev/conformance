metric is {
    name is "network_in"
    host is "server-13"
    timestamp is 1712805580 as u64
    value is 58.1
    unit is "bytes"
    tags is {
        environment is "production"
        region is "us-west"
        service is "svc-03"
    }
}
