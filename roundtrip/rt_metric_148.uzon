metric is {
    name is "open_files"
    host is "server-08"
    timestamp is 1712808880 as u64
    value is 51.6
    unit is "count"
    tags is {
        environment is "staging"
        region is "us-east"
        service is "svc-08"
    }
}
