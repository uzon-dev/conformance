// Schema definition #38
@type is "schema"
version is 4 as u8
name is "pi_schema_38"
fields are
        { name is "field_0", @type is "string", required is false },
        { name is "field_1", @type is "string", required is false },
        { name is "field_2", @type is "i32", required is false },
        { name is "field_3", @type is "i32", required is false }
