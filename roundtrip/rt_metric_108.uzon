metric is {
    name is "network_in"
    host is "server-08"
    timestamp is 1712806480 as u64
    value is 83.6
    unit is "bytes"
    tags is {
        environment is "production"
        region is "us-east"
        service is "svc-08"
    }
}
