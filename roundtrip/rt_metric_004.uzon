metric is {
    name is "network_out"
    host is "server-04"
    timestamp is 1712800240 as u64
    value is 6.8
    unit is "bytes"
    tags is {
        environment is "staging"
        region is "us-east"
        service is "svc-04"
    }
}
