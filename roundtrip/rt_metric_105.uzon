metric is {
    name is "cpu_usage"
    host is "server-05"
    timestamp is 1712806300 as u64
    value is 78.5
    unit is "percent"
    tags is {
        environment is "production"
        region is "us-west"
        service is "svc-05"
    }
}
