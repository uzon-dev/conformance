endpoints is [
    { path is "/kafka/api/v1/status", method is "GET", timeout_ms is 1600 as u32, auth is true },
    { path is "/kafka/api/v1/config", method is "GET", timeout_ms is 2600 as u32, auth is true },
    { path is "/kafka/api/v1/metrics", method is "GET", timeout_ms is 800 as u32, auth is false }
]
