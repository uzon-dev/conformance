metric is {
    name is "latency_p50"
    host is "server-17"
    timestamp is 1712809420 as u64
    value is 66.9
    unit is "milliseconds"
    tags is {
        environment is "staging"
        region is "us-west"
        service is "svc-07"
    }
}
