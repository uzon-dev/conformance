deployment is {
    id is "deploy-059"
    environment is "production"
    region is "ap-southeast-1"
    version is "3.19.4"
    replicas is 4 as u8
    image is "registry.example.com/app:3.19.4"
    resources is { cpu_m is 790 as u16, memory_mb is 1200 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-04T11:00:00Z"
    healthy is true
}
