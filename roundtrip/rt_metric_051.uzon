metric is {
    name is "error_rate"
    host is "server-11"
    timestamp is 1712803060 as u64
    value is 86.7
    unit is "percent"
    tags is {
        environment is "production"
        region is "ap-east"
        service is "svc-01"
    }
}
