deployment is {
    id is "deploy-093"
    environment is "staging"
    region is "ap-northeast-1"
    version is "5.13.3"
    replicas is 6 as u8
    image is "registry.example.com/app:5.13.3"
    resources is { cpu_m is 1130 as u16, memory_mb is 1744 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-10T21:00:00Z"
    healthy is true
}
