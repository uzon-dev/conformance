metric is {
    name is "disk_io"
    host is "server-12"
    timestamp is 1712801920 as u64
    value is 54.4
    unit is "bytes"
    tags is {
        environment is "development"
        region is "us-east"
        service is "svc-02"
    }
}
