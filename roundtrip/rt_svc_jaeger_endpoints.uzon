endpoints is [
    { path is "/jaeger/api/v1/status", method is "GET", timeout_ms is 2900 as u32, auth is false },
    { path is "/jaeger/api/v1/config", method is "GET", timeout_ms is 3900 as u32, auth is true },
    { path is "/jaeger/api/v1/metrics", method is "GET", timeout_ms is 1450 as u32, auth is false }
]
