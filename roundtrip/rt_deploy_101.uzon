deployment is {
    id is "deploy-101"
    environment is "staging"
    region is "us-west-2"
    version is "6.1.1"
    replicas is 6 as u8
    image is "registry.example.com/app:6.1.1"
    resources is { cpu_m is 1210 as u16, memory_mb is 1872 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-18T05:00:00Z"
    healthy is true
}
