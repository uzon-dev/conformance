deployment is {
    id is "deploy-075"
    environment is "production"
    region is "us-east-1"
    version is "4.15.0"
    replicas is 4 as u8
    image is "registry.example.com/app:4.15.0"
    resources is { cpu_m is 950 as u16, memory_mb is 1456 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-20T03:00:00Z"
    healthy is true
}
