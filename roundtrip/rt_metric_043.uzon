metric is {
    name is "open_files"
    host is "server-03"
    timestamp is 1712802580 as u64
    value is 73.1
    unit is "count"
    tags is {
        environment is "staging"
        region is "ap-east"
        service is "svc-03"
    }
}
