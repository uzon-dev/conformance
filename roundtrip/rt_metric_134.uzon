metric is {
    name is "heap_size"
    host is "server-14"
    timestamp is 1712808040 as u64
    value is 27.8
    unit is "bytes"
    tags is {
        environment is "development"
        region is "eu-west"
        service is "svc-04"
    }
}
