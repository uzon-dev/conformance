config is {
    app_name is "service-09"
    port is 3009 as u16
    debug is true
    log_level is "info"
    max_connections is 140 as u16
    timeout_sec is 75 as u16
    version is "1.9.0"
    region is "us-east"
}
