deployment is {
    id is "deploy-103"
    environment is "production"
    region is "ap-northeast-1"
    version is "6.3.3"
    replicas is 8 as u8
    image is "registry.example.com/app:6.3.3"
    resources is { cpu_m is 1230 as u16, memory_mb is 1904 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-20T07:00:00Z"
    healthy is true
}
