// Schema definition #127
@type is "schema"
version is 2 as u8
name is "staging_schema_127"
fields are
        { name is "field_0", @type is "f64", required is false },
        { name is "field_1", @type is "f64", required is true },
        { name is "field_2", @type is "u16", required is false },
        { name is "field_3", @type is "string", required is false },
        { name is "field_4", @type is "u16", required is true }
