endpoints is [
    { path is "/linkerd/api/v1/status", method is "GET", timeout_ms is 2400 as u32, auth is true },
    { path is "/linkerd/api/v1/config", method is "GET", timeout_ms is 3400 as u32, auth is true },
    { path is "/linkerd/api/v1/metrics", method is "GET", timeout_ms is 1200 as u32, auth is false }
]
