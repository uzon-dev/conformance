deployment is {
    id is "deploy-004"
    environment is "dev"
    region is "ap-southeast-1"
    version is "1.4.4"
    replicas is 5 as u8
    image is "registry.example.com/app:1.4.4"
    resources is { cpu_m is 240 as u16, memory_mb is 320 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-05T04:00:00Z"
    healthy is true
}
