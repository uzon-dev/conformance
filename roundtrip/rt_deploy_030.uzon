deployment is {
    id is "deploy-030"
    environment is "canary"
    region is "us-east-1"
    version is "2.10.0"
    replicas is 7 as u8
    image is "registry.example.com/app:2.10.0"
    resources is { cpu_m is 500 as u16, memory_mb is 736 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-03T06:00:00Z"
    healthy is true
}
