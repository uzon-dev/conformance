deployment is {
    id is "deploy-062"
    environment is "canary"
    region is "eu-west-1"
    version is "4.2.2"
    replicas is 7 as u8
    image is "registry.example.com/app:4.2.2"
    resources is { cpu_m is 820 as u16, memory_mb is 1248 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-07T14:00:00Z"
    healthy is true
}
