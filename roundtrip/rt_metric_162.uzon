metric is {
    name is "thread_count"
    host is "server-02"
    timestamp is 1712809720 as u64
    value is 75.4
    unit is "count"
    tags is {
        environment is "production"
        region is "eu-west"
        service is "svc-02"
    }
}
