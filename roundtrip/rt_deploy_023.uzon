deployment is {
    id is "deploy-023"
    environment is "production"
    region is "ap-northeast-1"
    version is "2.3.3"
    replicas is 8 as u8
    image is "registry.example.com/app:2.3.3"
    resources is { cpu_m is 430 as u16, memory_mb is 624 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-24T23:00:00Z"
    healthy is true
}
