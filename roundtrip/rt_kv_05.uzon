config is {
    app_name is "service-05"
    port is 3005 as u16
    debug is false
    log_level is "info"
    max_connections is 100 as u16
    timeout_sec is 55 as u16
    version is "1.5.0"
    region is "eu-west"
}
