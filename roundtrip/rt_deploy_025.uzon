deployment is {
    id is "deploy-025"
    environment is "staging"
    region is "us-east-1"
    version is "2.5.0"
    replicas is 2 as u8
    image is "registry.example.com/app:2.5.0"
    resources is { cpu_m is 450 as u16, memory_mb is 656 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-26T01:00:00Z"
    healthy is true
}
