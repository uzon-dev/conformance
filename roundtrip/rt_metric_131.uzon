metric is {
    name is "gc_pause"
    host is "server-11"
    timestamp is 1712807860 as u64
    value is 22.7
    unit is "milliseconds"
    tags is {
        environment is "development"
        region is "ap-east"
        service is "svc-01"
    }
}
