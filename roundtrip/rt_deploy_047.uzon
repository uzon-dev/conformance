deployment is {
    id is "deploy-047"
    environment is "production"
    region is "eu-west-1"
    version is "3.7.2"
    replicas is 8 as u8
    image is "registry.example.com/app:3.7.2"
    resources is { cpu_m is 670 as u16, memory_mb is 1008 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-20T23:00:00Z"
    healthy is true
}
