// Schema definition #129
@type is "schema"
version is 2 as u8
name is "theta_schema_129"
fields are
        { name is "field_0", @type is "string", required is true },
        { name is "field_1", @type is "string", required is false },
        { name is "field_2", @type is "u16", required is false }
