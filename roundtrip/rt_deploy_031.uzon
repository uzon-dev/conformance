deployment is {
    id is "deploy-031"
    environment is "production"
    region is "us-west-2"
    version is "2.11.1"
    replicas is 8 as u8
    image is "registry.example.com/app:2.11.1"
    resources is { cpu_m is 510 as u16, memory_mb is 752 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-04T07:00:00Z"
    healthy is true
}
