// Schema definition #99
@type is "schema"
version is 1 as u8
name is "mu_schema_99"
fields are
        { name is "field_0", @type is "f64", required is false },
        { name is "field_1", @type is "string", required is false }
