deployment is {
    id is "deploy-014"
    environment is "canary"
    region is "ap-southeast-1"
    version is "1.14.4"
    replicas is 7 as u8
    image is "registry.example.com/app:1.14.4"
    resources is { cpu_m is 340 as u16, memory_mb is 480 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-15T14:00:00Z"
    healthy is false
}
