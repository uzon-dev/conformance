deployment is {
    id is "deploy-033"
    environment is "staging"
    region is "ap-northeast-1"
    version is "2.13.3"
    replicas is 2 as u8
    image is "registry.example.com/app:2.13.3"
    resources is { cpu_m is 530 as u16, memory_mb is 784 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-06T09:00:00Z"
    healthy is true
}
