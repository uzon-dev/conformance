deployment is {
    id is "deploy-010"
    environment is "canary"
    region is "us-east-1"
    version is "1.10.0"
    replicas is 3 as u8
    image is "registry.example.com/app:1.10.0"
    resources is { cpu_m is 300 as u16, memory_mb is 416 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-11T10:00:00Z"
    healthy is true
}
