deployment is {
    id is "deploy-019"
    environment is "production"
    region is "ap-southeast-1"
    version is "1.19.4"
    replicas is 4 as u8
    image is "registry.example.com/app:1.19.4"
    resources is { cpu_m is 390 as u16, memory_mb is 560 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-20T19:00:00Z"
    healthy is true
}
