metric is {
    name is "open_files"
    host is "server-18"
    timestamp is 1712807080 as u64
    value is 0.6
    unit is "count"
    tags is {
        environment is "staging"
        region is "eu-west"
        service is "svc-08"
    }
}
