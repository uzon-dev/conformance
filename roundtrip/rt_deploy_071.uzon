deployment is {
    id is "deploy-071"
    environment is "production"
    region is "us-west-2"
    version is "4.11.1"
    replicas is 8 as u8
    image is "registry.example.com/app:4.11.1"
    resources is { cpu_m is 910 as u16, memory_mb is 1392 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-16T23:00:00Z"
    healthy is true
}
