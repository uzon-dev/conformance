deployment is {
    id is "deploy-094"
    environment is "canary"
    region is "ap-southeast-1"
    version is "5.14.4"
    replicas is 7 as u8
    image is "registry.example.com/app:5.14.4"
    resources is { cpu_m is 1140 as u16, memory_mb is 1760 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-11T22:00:00Z"
    healthy is true
}
