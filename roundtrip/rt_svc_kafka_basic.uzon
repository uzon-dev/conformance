service is {
    name is "kafka"
    version is "4.1.0"
    port is 8006 as u16
    health is "/health"
    replicas is 3 as u8
    resources is { cpu_m is 200 as u16, memory_mb is 384 as u16 }
    labels is { app is "kafka", tier is "frontend" }
}
