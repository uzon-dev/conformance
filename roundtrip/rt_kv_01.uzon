config is {
    app_name is "service-01"
    port is 3001 as u16
    debug is false
    log_level is "info"
    max_connections is 60 as u16
    timeout_sec is 35 as u16
    version is "1.1.0"
    region is "us-west"
}
