deployment is {
    id is "deploy-003"
    environment is "production"
    region is "ap-northeast-1"
    version is "1.3.3"
    replicas is 4 as u8
    image is "registry.example.com/app:1.3.3"
    resources is { cpu_m is 230 as u16, memory_mb is 304 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-04T03:00:00Z"
    healthy is true
}
