metric is {
    name is "network_in"
    host is "server-18"
    timestamp is 1712801080 as u64
    value is 30.6
    unit is "bytes"
    tags is {
        environment is "production"
        region is "eu-west"
        service is "svc-08"
    }
}
