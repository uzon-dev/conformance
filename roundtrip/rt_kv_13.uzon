config is {
    app_name is "service-13"
    port is 3013 as u16
    debug is false
    log_level is "info"
    max_connections is 180 as u16
    timeout_sec is 95 as u16
    version is "2.3.0"
    region is "us-west"
}
