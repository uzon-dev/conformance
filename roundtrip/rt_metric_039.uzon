metric is {
    name is "queue_depth"
    host is "server-19"
    timestamp is 1712802340 as u64
    value is 66.3
    unit is "count"
    tags is {
        environment is "production"
        region is "ap-east"
        service is "svc-09"
    }
}
