deployment is {
    id is "deploy-036"
    environment is "dev"
    region is "us-west-2"
    version is "2.16.1"
    replicas is 5 as u8
    image is "registry.example.com/app:2.16.1"
    resources is { cpu_m is 560 as u16, memory_mb is 832 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-09T12:00:00Z"
    healthy is true
}
