deployment is {
    id is "deploy-070"
    environment is "canary"
    region is "us-east-1"
    version is "4.10.0"
    replicas is 7 as u8
    image is "registry.example.com/app:4.10.0"
    resources is { cpu_m is 900 as u16, memory_mb is 1376 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-15T22:00:00Z"
    healthy is false
}
