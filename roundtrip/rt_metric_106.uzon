metric is {
    name is "memory_pct"
    host is "server-06"
    timestamp is 1712806360 as u64
    value is 80.2
    unit is "percent"
    tags is {
        environment is "staging"
        region is "eu-west"
        service is "svc-06"
    }
}
