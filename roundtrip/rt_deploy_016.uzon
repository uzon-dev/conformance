deployment is {
    id is "deploy-016"
    environment is "dev"
    region is "us-west-2"
    version is "1.16.1"
    replicas is 1 as u8
    image is "registry.example.com/app:1.16.1"
    resources is { cpu_m is 360 as u16, memory_mb is 512 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-17T16:00:00Z"
    healthy is true
}
