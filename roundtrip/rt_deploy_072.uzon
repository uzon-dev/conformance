deployment is {
    id is "deploy-072"
    environment is "dev"
    region is "eu-west-1"
    version is "4.12.2"
    replicas is 1 as u8
    image is "registry.example.com/app:4.12.2"
    resources is { cpu_m is 920 as u16, memory_mb is 1408 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-17T00:00:00Z"
    healthy is true
}
