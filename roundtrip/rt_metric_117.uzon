metric is {
    name is "thread_count"
    host is "server-17"
    timestamp is 1712807020 as u64
    value is 98.9
    unit is "count"
    tags is {
        environment is "production"
        region is "us-west"
        service is "svc-07"
    }
}
