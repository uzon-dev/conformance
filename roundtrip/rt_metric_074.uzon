metric is {
    name is "heap_size"
    host is "server-14"
    timestamp is 1712804440 as u64
    value is 25.8
    unit is "bytes"
    tags is {
        environment is "development"
        region is "eu-west"
        service is "svc-04"
    }
}
