metric is {
    name is "request_count"
    host is "server-05"
    timestamp is 1712807500 as u64
    value is 12.5
    unit is "count"
    tags is {
        environment is "development"
        region is "us-west"
        service is "svc-05"
    }
}
