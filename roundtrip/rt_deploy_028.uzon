deployment is {
    id is "deploy-028"
    environment is "dev"
    region is "ap-northeast-1"
    version is "2.8.3"
    replicas is 5 as u8
    image is "registry.example.com/app:2.8.3"
    resources is { cpu_m is 480 as u16, memory_mb is 704 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-01T04:00:00Z"
    healthy is false
}
