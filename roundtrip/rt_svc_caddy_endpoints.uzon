endpoints is [
    { path is "/caddy/api/v1/status", method is "GET", timeout_ms is 2100 as u32, auth is false },
    { path is "/caddy/api/v1/config", method is "GET", timeout_ms is 3100 as u32, auth is true },
    { path is "/caddy/api/v1/metrics", method is "GET", timeout_ms is 1050 as u32, auth is false }
]
