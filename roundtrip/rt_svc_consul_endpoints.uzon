endpoints is [
    { path is "/consul/api/v1/status", method is "GET", timeout_ms is 1800 as u32, auth is true },
    { path is "/consul/api/v1/config", method is "GET", timeout_ms is 2800 as u32, auth is true },
    { path is "/consul/api/v1/metrics", method is "GET", timeout_ms is 900 as u32, auth is false }
]
