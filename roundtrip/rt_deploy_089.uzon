deployment is {
    id is "deploy-089"
    environment is "staging"
    region is "ap-southeast-1"
    version is "5.9.4"
    replicas is 2 as u8
    image is "registry.example.com/app:5.9.4"
    resources is { cpu_m is 1090 as u16, memory_mb is 1680 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-06T17:00:00Z"
    healthy is true
}
