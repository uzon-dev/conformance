config is {
    app_name is "service-03"
    port is 3003 as u16
    debug is true
    log_level is "error"
    max_connections is 80 as u16
    timeout_sec is 45 as u16
    version is "1.3.0"
    region is "us-east"
}
