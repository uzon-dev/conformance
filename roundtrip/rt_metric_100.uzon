metric is {
    name is "connection_pool"
    host is "server-00"
    timestamp is 1712806000 as u64
    value is 70.0
    unit is "bytes"
    tags is {
        environment is "staging"
        region is "us-east"
        service is "svc-00"
    }
}
