endpoints is [
    { path is "/redis/api/v1/status", method is "GET", timeout_ms is 1100 as u32, auth is false },
    { path is "/redis/api/v1/config", method is "GET", timeout_ms is 2100 as u32, auth is true },
    { path is "/redis/api/v1/metrics", method is "GET", timeout_ms is 550 as u32, auth is false }
]
