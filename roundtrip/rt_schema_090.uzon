// Schema definition #90
@type is "schema"
version is 5 as u8
name is "dev_schema_90"
fields are
        { name is "field_0", @type is "string", required is false },
        { name is "field_1", @type is "i32", required is true }
