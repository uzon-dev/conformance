deployment is {
    id is "deploy-106"
    environment is "canary"
    region is "us-west-2"
    version is "6.6.1"
    replicas is 3 as u8
    image is "registry.example.com/app:6.6.1"
    resources is { cpu_m is 1260 as u16, memory_mb is 1952 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-23T10:00:00Z"
    healthy is true
}
