metric is {
    name is "thread_count"
    host is "server-17"
    timestamp is 1712803420 as u64
    value is 96.9
    unit is "count"
    tags is {
        environment is "production"
        region is "us-west"
        service is "svc-07"
    }
}
