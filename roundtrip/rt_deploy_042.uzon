deployment is {
    id is "deploy-042"
    environment is "canary"
    region is "eu-west-1"
    version is "3.2.2"
    replicas is 3 as u8
    image is "registry.example.com/app:3.2.2"
    resources is { cpu_m is 620 as u16, memory_mb is 928 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-15T18:00:00Z"
    healthy is false
}
