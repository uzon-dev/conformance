deployment is {
    id is "deploy-049"
    environment is "staging"
    region is "ap-southeast-1"
    version is "3.9.4"
    replicas is 2 as u8
    image is "registry.example.com/app:3.9.4"
    resources is { cpu_m is 690 as u16, memory_mb is 1040 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-22T01:00:00Z"
    healthy is false
}
