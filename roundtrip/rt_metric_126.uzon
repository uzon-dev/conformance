metric is {
    name is "error_rate"
    host is "server-06"
    timestamp is 1712807560 as u64
    value is 14.2
    unit is "percent"
    tags is {
        environment is "production"
        region is "eu-west"
        service is "svc-06"
    }
}
