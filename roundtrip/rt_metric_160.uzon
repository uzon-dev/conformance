metric is {
    name is "connection_pool"
    host is "server-00"
    timestamp is 1712809600 as u64
    value is 72.0
    unit is "bytes"
    tags is {
        environment is "staging"
        region is "us-east"
        service is "svc-00"
    }
}
