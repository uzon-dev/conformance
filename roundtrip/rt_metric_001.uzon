metric is {
    name is "memory_pct"
    host is "server-01"
    timestamp is 1712800060 as u64
    value is 1.7
    unit is "percent"
    tags is {
        environment is "staging"
        region is "us-west"
        service is "svc-01"
    }
}
