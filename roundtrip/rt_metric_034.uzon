metric is {
    name is "network_out"
    host is "server-14"
    timestamp is 1712802040 as u64
    value is 57.8
    unit is "bytes"
    tags is {
        environment is "staging"
        region is "eu-west"
        service is "svc-04"
    }
}
