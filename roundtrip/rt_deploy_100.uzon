deployment is {
    id is "deploy-100"
    environment is "dev"
    region is "us-east-1"
    version is "6.0.0"
    replicas is 5 as u8
    image is "registry.example.com/app:6.0.0"
    resources is { cpu_m is 1200 as u16, memory_mb is 1856 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-17T04:00:00Z"
    healthy is true
}
