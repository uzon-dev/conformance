metric is {
    name is "cpu_usage"
    host is "server-05"
    timestamp is 1712802700 as u64
    value is 76.5
    unit is "percent"
    tags is {
        environment is "production"
        region is "us-west"
        service is "svc-05"
    }
}
