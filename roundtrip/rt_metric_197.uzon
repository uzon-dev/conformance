metric is {
    name is "disk_io"
    host is "server-17"
    timestamp is 1712811820 as u64
    value is 34.9
    unit is "bytes"
    tags is {
        environment is "development"
        region is "us-west"
        service is "svc-07"
    }
}
