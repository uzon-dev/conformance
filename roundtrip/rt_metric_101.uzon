metric is {
    name is "gc_pause"
    host is "server-01"
    timestamp is 1712806060 as u64
    value is 71.7
    unit is "milliseconds"
    tags is {
        environment is "development"
        region is "us-west"
        service is "svc-01"
    }
}
