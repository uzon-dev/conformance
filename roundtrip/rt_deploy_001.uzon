deployment is {
    id is "deploy-001"
    environment is "staging"
    region is "us-west-2"
    version is "1.1.1"
    replicas is 2 as u8
    image is "registry.example.com/app:1.1.1"
    resources is { cpu_m is 210 as u16, memory_mb is 272 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-02T01:00:00Z"
    healthy is true
}
