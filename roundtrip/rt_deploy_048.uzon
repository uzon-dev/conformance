deployment is {
    id is "deploy-048"
    environment is "dev"
    region is "ap-northeast-1"
    version is "3.8.3"
    replicas is 1 as u8
    image is "registry.example.com/app:3.8.3"
    resources is { cpu_m is 680 as u16, memory_mb is 1024 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-21T00:00:00Z"
    healthy is true
}
