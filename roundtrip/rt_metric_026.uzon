metric is {
    name is "gc_pause"
    host is "server-06"
    timestamp is 1712801560 as u64
    value is 44.2
    unit is "milliseconds"
    tags is {
        environment is "development"
        region is "eu-west"
        service is "svc-06"
    }
}
