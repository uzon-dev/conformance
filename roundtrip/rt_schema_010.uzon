// Schema definition #10
@type is "schema"
version is 3 as u8
name is "node_schema_10"
fields are
        { name is "field_0", @type is "i64", required is false },
        { name is "field_1", @type is "u16", required is false }
