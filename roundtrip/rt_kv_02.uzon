config is {
    app_name is "service-02"
    port is 3002 as u16
    debug is false
    log_level is "warn"
    max_connections is 70 as u16
    timeout_sec is 40 as u16
    version is "1.2.0"
    region is "eu-west"
}
