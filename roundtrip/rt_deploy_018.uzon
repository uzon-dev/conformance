deployment is {
    id is "deploy-018"
    environment is "canary"
    region is "ap-northeast-1"
    version is "1.18.3"
    replicas is 3 as u8
    image is "registry.example.com/app:1.18.3"
    resources is { cpu_m is 380 as u16, memory_mb is 544 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-19T18:00:00Z"
    healthy is true
}
