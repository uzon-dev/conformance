metric is {
    name is "network_out"
    host is "server-09"
    timestamp is 1712806540 as u64
    value is 85.3
    unit is "bytes"
    tags is {
        environment is "staging"
        region is "us-west"
        service is "svc-09"
    }
}
