// Schema definition #141
@type is "schema"
version is 3 as u8
name is "level_schema_141"
fields are
        { name is "field_0", @type is "string", required is true },
        { name is "field_1", @type is "bool", required is true }
