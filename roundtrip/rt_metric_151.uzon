metric is {
    name is "memory_pct"
    host is "server-11"
    timestamp is 1712809060 as u64
    value is 56.7
    unit is "percent"
    tags is {
        environment is "staging"
        region is "ap-east"
        service is "svc-01"
    }
}
