// Schema definition #161
@type is "schema"
version is 2 as u8
name is "iota_schema_161"
fields are
        { name is "field_0", @type is "u16", required is false },
        { name is "field_1", @type is "i64", required is true }
