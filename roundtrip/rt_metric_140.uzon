metric is {
    name is "request_count"
    host is "server-00"
    timestamp is 1712808400 as u64
    value is 38.0
    unit is "count"
    tags is {
        environment is "development"
        region is "us-east"
        service is "svc-00"
    }
}
