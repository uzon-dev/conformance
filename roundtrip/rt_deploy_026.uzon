deployment is {
    id is "deploy-026"
    environment is "canary"
    region is "us-west-2"
    version is "2.6.1"
    replicas is 3 as u8
    image is "registry.example.com/app:2.6.1"
    resources is { cpu_m is 460 as u16, memory_mb is 672 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-27T02:00:00Z"
    healthy is true
}
