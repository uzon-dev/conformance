deployment is {
    id is "deploy-008"
    environment is "dev"
    region is "ap-northeast-1"
    version is "1.8.3"
    replicas is 1 as u8
    image is "registry.example.com/app:1.8.3"
    resources is { cpu_m is 280 as u16, memory_mb is 384 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-09T08:00:00Z"
    healthy is true
}
