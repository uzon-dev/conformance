deployment is {
    id is "deploy-095"
    environment is "production"
    region is "us-east-1"
    version is "5.15.0"
    replicas is 8 as u8
    image is "registry.example.com/app:5.15.0"
    resources is { cpu_m is 1150 as u16, memory_mb is 1776 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-12T23:00:00Z"
    healthy is true
}
