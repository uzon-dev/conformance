metric is {
    name is "cpu_usage"
    host is "server-10"
    timestamp is 1712809000 as u64
    value is 55.0
    unit is "percent"
    tags is {
        environment is "production"
        region is "eu-west"
        service is "svc-00"
    }
}
