deployment is {
    id is "deploy-087"
    environment is "production"
    region is "eu-west-1"
    version is "5.7.2"
    replicas is 8 as u8
    image is "registry.example.com/app:5.7.2"
    resources is { cpu_m is 1070 as u16, memory_mb is 1648 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-04T15:00:00Z"
    healthy is true
}
