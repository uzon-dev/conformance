deployment is {
    id is "deploy-007"
    environment is "production"
    region is "eu-west-1"
    version is "1.7.2"
    replicas is 8 as u8
    image is "registry.example.com/app:1.7.2"
    resources is { cpu_m is 270 as u16, memory_mb is 368 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 1 as u8 }
    created_at is "2026-04-08T07:00:00Z"
    healthy is false
}
