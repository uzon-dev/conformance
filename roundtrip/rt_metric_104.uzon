metric is {
    name is "heap_size"
    host is "server-04"
    timestamp is 1712806240 as u64
    value is 76.8
    unit is "bytes"
    tags is {
        environment is "development"
        region is "us-east"
        service is "svc-04"
    }
}
