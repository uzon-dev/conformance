deployment is {
    id is "deploy-024"
    environment is "dev"
    region is "ap-southeast-1"
    version is "2.4.4"
    replicas is 1 as u8
    image is "registry.example.com/app:2.4.4"
    resources is { cpu_m is 440 as u16, memory_mb is 640 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-25T00:00:00Z"
    healthy is true
}
