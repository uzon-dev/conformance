metric is {
    name is "latency_p99"
    host is "server-08"
    timestamp is 1712800480 as u64
    value is 13.6
    unit is "milliseconds"
    tags is {
        environment is "development"
        region is "us-east"
        service is "svc-08"
    }
}
