metric is {
    name is "latency_p50"
    host is "server-12"
    timestamp is 1712806720 as u64
    value is 90.4
    unit is "milliseconds"
    tags is {
        environment is "staging"
        region is "us-east"
        service is "svc-02"
    }
}
