endpoints is [
    { path is "/mongo/api/v1/status", method is "GET", timeout_ms is 1400 as u32, auth is true },
    { path is "/mongo/api/v1/config", method is "GET", timeout_ms is 2400 as u32, auth is true },
    { path is "/mongo/api/v1/metrics", method is "GET", timeout_ms is 700 as u32, auth is false }
]
