// Schema definition #64
@type is "schema"
version is 4 as u8
name is "host_schema_64"
fields are
        { name is "field_0", @type is "bool", required is true },
        { name is "field_1", @type is "string", required is true },
        { name is "field_2", @type is "string", required is false }
