metric is {
    name is "gc_pause"
    host is "server-16"
    timestamp is 1712810560 as u64
    value is 99.2
    unit is "milliseconds"
    tags is {
        environment is "development"
        region is "us-east"
        service is "svc-06"
    }
}
