metric is {
    name is "request_count"
    host is "server-15"
    timestamp is 1712809300 as u64
    value is 63.5
    unit is "count"
    tags is {
        environment is "development"
        region is "ap-east"
        service is "svc-05"
    }
}
