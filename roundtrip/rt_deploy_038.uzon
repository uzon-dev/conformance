deployment is {
    id is "deploy-038"
    environment is "canary"
    region is "ap-northeast-1"
    version is "2.18.3"
    replicas is 7 as u8
    image is "registry.example.com/app:2.18.3"
    resources is { cpu_m is 580 as u16, memory_mb is 864 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-11T14:00:00Z"
    healthy is true
}
