config is {
    app_name is "service-19"
    port is 3019 as u16
    debug is false
    log_level is "error"
    max_connections is 240 as u16
    timeout_sec is 125 as u16
    version is "2.9.0"
    region is "us-west"
}
