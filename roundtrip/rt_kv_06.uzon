config is {
    app_name is "service-06"
    port is 3006 as u16
    debug is true
    log_level is "warn"
    max_connections is 110 as u16
    timeout_sec is 60 as u16
    version is "1.6.0"
    region is "us-east"
}
