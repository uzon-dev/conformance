endpoints is [
    { path is "/prometheus/api/v1/status", method is "GET", timeout_ms is 2600 as u32, auth is true },
    { path is "/prometheus/api/v1/config", method is "GET", timeout_ms is 3600 as u32, auth is true },
    { path is "/prometheus/api/v1/metrics", method is "GET", timeout_ms is 1300 as u32, auth is false }
]
