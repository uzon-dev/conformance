deployment is {
    id is "deploy-040"
    environment is "dev"
    region is "us-east-1"
    version is "3.0.0"
    replicas is 1 as u8
    image is "registry.example.com/app:3.0.0"
    resources is { cpu_m is 600 as u16, memory_mb is 896 as u16 }
    rollout is { max_surge is 2 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-13T16:00:00Z"
    healthy is true
}
