deployment is {
    id is "deploy-012"
    environment is "dev"
    region is "eu-west-1"
    version is "1.12.2"
    replicas is 5 as u8
    image is "registry.example.com/app:1.12.2"
    resources is { cpu_m is 320 as u16, memory_mb is 448 as u16 }
    rollout is { max_surge is 1 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-13T12:00:00Z"
    healthy is true
}
