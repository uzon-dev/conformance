// Resource #64
@type is "queue"
name is "queue_64"
region is "ap-northeast-1"
config is {
    @type is "queue_config"
    broker is "nats"
    max_retries is 8 as u8
    batch_size is 473 as u16
}
active is true
