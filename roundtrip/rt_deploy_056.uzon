deployment is {
    id is "deploy-056"
    environment is "dev"
    region is "us-west-2"
    version is "3.16.1"
    replicas is 1 as u8
    image is "registry.example.com/app:3.16.1"
    resources is { cpu_m is 760 as u16, memory_mb is 1152 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-01T08:00:00Z"
    healthy is false
}
