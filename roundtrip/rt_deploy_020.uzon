deployment is {
    id is "deploy-020"
    environment is "dev"
    region is "us-east-1"
    version is "2.0.0"
    replicas is 5 as u8
    image is "registry.example.com/app:2.0.0"
    resources is { cpu_m is 400 as u16, memory_mb is 576 as u16 }
    rollout is { max_surge is 3 as u8, max_unavailable is 0 as u8 }
    created_at is "2026-04-21T20:00:00Z"
    healthy is true
}
