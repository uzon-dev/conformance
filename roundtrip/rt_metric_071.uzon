metric is {
    name is "gc_pause"
    host is "server-11"
    timestamp is 1712804260 as u64
    value is 20.7
    unit is "milliseconds"
    tags is {
        environment is "development"
        region is "ap-east"
        service is "svc-01"
    }
}
