// Schema definition #151
@type is "schema"
version is 4 as u8
name is "iota_schema_151"
fields are
        { name is "field_0", @type is "string", required is true },
        { name is "field_1", @type is "i32", required is true }
