metric is {
    name is "network_out"
    host is "server-19"
    timestamp is 1712808340 as u64
    value is 36.3
    unit is "bytes"
    tags is {
        environment is "staging"
        region is "ap-east"
        service is "svc-09"
    }
}
