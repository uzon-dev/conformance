from_list is std.get([ 10, 20, 30 ], 1)
from_struct is std.get({ port is 8080 }, "port")
out_of_bounds is std.get([ 10 ], 5) or else "missing"
missing_key is std.get({ a is 1 }, "z") or else "missing"
