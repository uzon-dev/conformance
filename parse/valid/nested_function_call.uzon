// §5.15 Nested function calls
double is function n as i32 returns i32 { n * 2 }
inc is function n as i32 returns i32 { n + 1 }

// f(g(x))
result is double(inc(3))    // double(4) = 8
chained is inc(inc(inc(0)))  // 3
