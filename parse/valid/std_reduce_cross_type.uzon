// §5.16.4 std.reduce element type T and accumulator type U may differ
words are "hello", "world", "uzon"
total_len is std.reduce(words, 0, function acc as i64, w as string returns i64 {
    acc + std.len(w)
})
