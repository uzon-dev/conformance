// ERROR: §5.16.2 + §3.8
// std.filter function must return bool, not i32
_shared is struct "./shared"

modules are _shared.bridge, _shared.base_module

bad is std.filter(
    modules,
    function m as _shared.BaseModule returns i32 { m.power_kw to i32 }
)
