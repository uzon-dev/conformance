// §3.2.1 + §3.7.1: with requires struct, tagged union is not transparent here
result is "ok" named ok from ok as string, err as string called Result
bad is result with { x is 1 }    // tagged union, not struct
