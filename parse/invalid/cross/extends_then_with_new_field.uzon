// §3.2.1 + §3.2.2: plus then with — with cannot add new fields
base is { x is 1 as i32 }
extended is base plus { y is 2 as i32 }
// extended has { x, y } — with can only override existing fields
bad is extended with { z is 3 as i32 }
