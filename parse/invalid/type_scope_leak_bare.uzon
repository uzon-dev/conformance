// §6.2: Type defined inside struct — bare reference is a type error
outer is {
    inner is green from green, red, blue called Color
}
// Color is scoped to outer — must use outer.Color
x is red as Color
